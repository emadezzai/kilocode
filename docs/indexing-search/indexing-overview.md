# الفهرسة والبحث - نظرة عامة

> **الميزة:** الفهرسة الذكية والبحث المتقدم  
> **الأولوية:** P1 (عالية)  
> **الحالة:** مكتملة  
> **تاريخ الإكمال:** 9 يناير 2026

## 📋 ملخص

تتميز ميزة الفهرسة والبحث في Kilo Code بقدرتها على فهرسة تلقائية لجميع عناصر الكود وتوفير بحث متقدم بالمعنى والنص. هذه الميزة تحول تجربة البحث في الكود من البحث التقليدي إلى البحث الذكي الذي يفهم السياق والعلاقات.

## 🌟 الميزات الرئيسية

### 🤖 الفهرسة الذكية

- **الفهرسة التلقائية:** فهرسة تلقائية لجميع عناصر الكود (functions, classes, variables, etc.)
- **تحديث في الوقت الفعلي:** تحديث تلقائي عند تغيير الملفات
- **دعم لغات متعددة:** دعم TypeScript, JavaScript, Python, Java, C++, Go, Rust, Kotlin, Swift, R, PHP, Ruby
- **استخراج العلاقات:** اكتشاف العلاقات بين عناصر الكود (imports, dependencies, usage)
- **التجزئة الذكية:** تقسيم الكود إلى أجزاء قابلة للبحث

### 🔍 البحث المتقدم

- **البحث بالنص:** بحث سريع بالكلمات الرئيسية
- **البحث الدلالي:** بحث بالمعنى باستخدام vector embeddings
- **البحث الهجين:** دمج بين البحث بالنص والدلالي للحصول على أفضل النتائج
- **البحث الضبابي:** معالجة الأخطاء الإملائية والبحث التقريبي
- **التصفية المتقدمة:** تصفية حسب نوع الملف، اللغة، التاريخ، المؤلف
- **الترحيل:** دعم مجموعات النتائج الكبيرة

### 🎯 الاكتشاف الذكي

- **اكتشاف العلاقات:** اكتشاف تلقائي للعناصر ذات الصلة
- **الأنماط المتكررة:** التعرف على الأنماط المتكررة في الكود
- **الاقتراحات الذكية:** اقتراحات قائمة على السياق الحالي
- **التمييز:** تمييز النتائج المطابقة في الكود
- **الترتيب بالصلة:** ترتيب النتائج حسب الصلة والسياق

## 🏗️ البنية المعمارية

### المكونات الأساسية

```
src/services/indexing/
├── indexing-service.ts     # خدمة الفهرسة الرئيسية
├── search-engine.ts        # محرك البحث المتقدم
├── code-indexer.ts         # فهرس الكود
├── database-manager.ts     # مدير قاعدة البيانات
├── config-manager.ts       # مدير الإعدادات
├── error-handler.ts        # معالج الأخطاء
└── logging-service.ts      # خدمة التسجيل
```

### قاعدة البيانات

- **SQLite:** قاعدة البيانات الرئيسية للفهرسة
- **LanceDB/Qdrant:** قاعدة بيانات vector للبحث الدلالي
- **الجداول الرئيسية:**
    - `code_elements`: عناصر الكود المفهرسة
    - `code_relationships`: العلاقات بين عناصر الكود
    - `search_index`: فهرس البحث
    - `vector_embeddings`: embeddings للبحث الدلالي

### الأدوات

```
src/core/tools/
└── indexing-tool.ts         # أداة VS Code للفهرسة
```

## 🚀 الأداء

### أهداف الأداء

- **فهرسة الكود:** <50ms لكل ملف
- **البحث بالنص:** <2 ثانية
- **البحث الدلالي:** <3 ثانية
- **البحث الهجين:** <2.5 ثانية
- **العمليات المتزامنة:** 20+ عملية بحث متزامنة

### التحسينات

- **التخزين المؤقت:** نتائج البحث المخزنة مؤقتاً
- **الفهرسة التدريجية:** فهرسة تدريجية للملفات الكبيرة
- **التحميل المتوازي:** تحميل متوازي للembeddings
- **تحسين الذاكرة:** استخدام فعال للذاكرة

## 🔧 التكامل

### مع VS Code

- **الأوامر الجديدة:**

    - `kilo.indexWorkspace` - فهرسة مساحة العمل
    - `kilo.searchCode` - بحث في الكود
    - `kilo.semanticSearch` - بحث دلالي
    - `kilo.hybridSearch` - بحث هجين

- **الإعدادات:**
    - `kilo.indexing.enabled` - تفعيل الفهرسة
    - `kilo.indexing.excludePatterns` - أنماط الاستبعاد
    - `kilo.search.maxResults` - الحد الأقصى للنتائج
    - `kilo.search.minScore` - الحد الأدنى للنتيجة

### مع الخدمات الأخرى

- **خدمة الذاكرة:** تكامل مع الذاكرة لتوفير سياق البحث
- **خدمة التحليلات:** تتبع استخدام البحث وتحليل الأداء
- **مدير الإعدادات:** إدارة الإعدادات المشتركة

## 📊 الاستخدام

### أمثلة الاستخدام

```typescript
// البحث بالنص
const textResults = await searchEngine.search({
	queryText: "function calculateTotal",
	searchType: "text",
	maxResults: 50,
})

// البحث الدلالي
const semanticResults = await searchEngine.search({
	queryText: "mathematical operations",
	searchType: "semantic",
	maxResults: 50,
})

// البحث الهجين
const hybridResults = await searchEngine.search({
	queryText: "React component with hooks",
	searchType: "hybrid",
	maxResults: 50,
})
```

### أفضل الممارسات

- **استخدام كلمات مفتاحية محددة** للبحث بالنص
- **استخدام أوصاف مفصلة** للبحث الدلالي
- **استخدام البحث الهجين** للحصول على أفضل النتائج
- **استخدام التصفية** لتقليل النتائج غير ذات الصلة

## 🔍 استكشاف الأخطاء

### المشاكل الشائعة

- **الفهرسة البطيئة:** تحقق من حجم الملفات والإعدادات
- **نتائج البحث الفارغة:** تحقق من استعلامات البحث والفهرسة
- **مشاكل الذاكرة:** تحقق من إعدادات التخزين المؤقت
- **مشاكل التكامل:** تحقق من تثبيت VS Code والإعدادات

### الحلول

- **إعادة الفهرسة:** استخدم أمر إعادة الفهرسة
- **فحص الإعدادات:** تحقق من إعدادات الفهرسة والبحث
- **مراجعة السجلات:** تحقق من سجلات الأخطاء
- **تحديث الإضافات:** تأكد من تحديث الإضافات

## 📈 الإحصائيات

- **عدد اللغات المدعومة:** 15+ لغة برمجة
- **أنواع العناصر المدعومة:** 10+ (functions, classes, variables, etc.)
- **حجم قاعدة البيانات:** قابل للتوسعة مع SQLite
- **دعم الملفات:** 100+ نوع ملف
- **الأداء:** بحث في 1000+ ملف في <2 ثانية

## 🎯 التطوير المستقبلي

### الميزات المخططة

- **البحث الصوتي:** بحث باستخدام الأوامر الصوتية
- **البحث البصري:** بحث باستخدام الصور والرسوم البيانية
- **التعلم الآلي:** تحسين البحث باستخدام التعلم الآلي
- **البحث متعدد اللغات:** بحث في لغات برمجة متعددة

### التحسينات

- **تحسين الأداء:** تحسين سرعة البحث والفهرسة
- **تحسين الواجهة:** تحسين واجهة البحث والنتائج
- **تحسين التكامل:** تحسين التكامل مع VS Code
- **تحسين التوثيق:** تحسين التوثيق والأمثلة

---

**آخر تحديث:** 9 يناير 2026  
**المسؤول:** فريق تطوير Kilo Code  
**المرخصة:** MIT
